# Code-Server Configuration

# Access password for code-server
PASSWORD=your_secure_password

# Sudo password for terminal operations
SUDO_PASSWORD=your_sudo_password

# Port to expose code-server (default: 8080)
PORT=8080

# Timezone setting
TZ=Asia/Tokyo

# Default workspace directory inside container
DEFAULT_WORKSPACE=/home/coder/project

# User and Group IDs (default: 1000)
PUID=1000
PGID=1000